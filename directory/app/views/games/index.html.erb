<%= link_to "Add a game", new_game_path %>
<div class="games-page">
  <% @games.order('name').each do |game| %>
    <h4><%= game.name %></h4>
    <img src="<%= game.imgurl %>" style="width: 200px">
    <p>
      <%= link_to "More info", game_path(game) %>
    </p>
  <% end %>
</div>
